name: orgFeeder
run-name: ${{github.actor}} is running Github Actions...
on: [pull_request, push]
env:
  SECRET_JWT: "MAN CITY"
  EMAIL: "on.screen.keyboards@gmail.com"
  PASSWORD: tjeekcazmvpewaku
  MONGO_URI: "mongodb+srv://admin:admin@bindeshcluster.uev3zzw.mongodb.net/?retryWrites=true&w=majority"
  MONGO_TEST_URI: "mongodb+srv://admin:admin@bindeshcluster.uev3zzw.mongodb.net/jest?retryWrites=true&w=majority"
jobs:
  run-jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: echo ${{secrets.MONGO_TEST_URI}}
      - run: npm test user.test.js
